#language = "V8"
#title = "", 1028, "", 2052, "S2TWP", 1033
#tooltip = "Convert to Taiwan Phrases", 1033, "转台湾用语", 2052, "轉台灣用語", 1028
#icon="S2TWP.ico"

/**
 * Project: OpenCC for EmEditor Macro v0.19
 * Author: skyuns (https://github.com/skyuns/EmEditor-OpenCC-Macro)
 * This macro provides foundational conversion based on the OpenCC core lexicon. Note that results may vary from the official implementation.
 * (本巨集基於 OpenCC 核心詞庫提供基礎繁簡轉換。請注意，轉換結果可能與官方執行版本有所出入。)
 * Data Source: OpenCC (https://github.com/BYVoid/OpenCC)
 * License: Apache License 2.0
 * 轉換流程：1. 讀取用語字典 -> 2. 封裝 Trie 字典樹 -> 3. 動態分段由下往上執行 FMM 匹配
 * 簡易測試：由於內存不足，導致高清視頻的優化過程卡死，連硬盤裡的文件都無法讀取。
 * 參考結果：由於記憶體不足，導致高畫質影片的最佳化過程卡死，連硬碟裡的檔案都無法讀取。 (僅做用語轉換)
 */

const CURRENT_MODE = "S2TWP"; 

// --- [區塊 A：大陸用語 -> 台灣用語 (TWPhrases.txt)] ---
const rawPhraseData = `
# Open Chinese Convert (OpenCC) Dictionary
# File: TWPhrases.txt
# Format: key	value(s) (values separated by spaces)
# License: Apache-2.0 (see LICENSE)
# Source: https://github.com/ByVoid/OpenCC
# Used in configs: s2twp.json (via TWPhrases.ocd2)

PN結	PN接面
SQL注入	SQL隱碼攻擊
SQL注入攻擊	SQL隱碼攻擊
U盤	隨身碟
三極管	三極體
下拉列表	下拉式清單
並行計算	平行計算
中間件	中介軟體
串口	序列埠
串行	序列
串行端口	序列埠
主引導記錄	主開機記錄
主板	主機板
乍得	查德
也門	葉門
二極管	二極體
互聯網	網際網路
交互	互動
交互式	互動式
人工智能	人工智慧
仙童半導體	快捷半導體
代碼	程式碼 代碼
代碼頁	內碼表
以太網	乙太網
任務欄	工作列
任務管理器	工作管理員
仿真	模擬
伯利茲	貝里斯
位圖	點陣圖
低級	低階 低級
佛得角	維德角
便攜式	行動式 攜帶型
保存	儲存
信噪比	訊雜比
信息	資訊
信息安全	資訊安全
信息技術	資訊科技
信息論	資訊理論
信號	訊號 信號
信道	通道
借記卡	簽帳金融卡
傅里葉	傅立葉
傳感	感測
像素	畫素
僞代碼	虛擬碼
優先級	優先順序
優化	最佳化
元數據	後設資料
元編程	超程式設計
元音	母音
光標	游標
光盤	光碟
光驅	光碟機
克羅地亞	克羅埃西亞
免提	擴音
內存	記憶體
內核	核心
內置	內建
內聯函數	行內函數
全局	全域性
全角	全形
兼容	相容
冒泡排序	氣泡排序
冰棍	冰棒
出租車	計程車
函數	函式
函數式編程	函數語言程式設計
刀片服務器	刀鋒伺服器
分佈式	分散式
分區	分割槽
分辨率	解析度
列支敦士登	列支敦斯登
利比里亞	賴比瑞亞
刷新	重新整理
刻錄	燒錄
前列腺	攝護腺
前綴	字首
剪切	剪下
剪貼板	剪貼簿
創建	建立
加納	迦納
加蓬	加彭
加載	載入
半角	半形
博客	部落格
博茨瓦納	波札那
卡塔爾	卡達
危地馬拉	瓜地馬拉
卸載	解除安裝
厄瓜多爾	厄瓜多
厄立特里亞	厄利垂亞
原代碼	原始碼
參數	引數
參數表	參數列
句柄	控制代碼
可視化	視覺化
吉布堤	吉布地
呼出	撥出
呼叫轉移	來電轉接
命令式編程	指令式程式設計
命令行	命令列
命名空間	名稱空間 名字空間
咖喱	咖哩
哈希	雜湊
哈薩克斯坦	哈薩克
哥斯達黎加	哥斯大黎加
喫茶小舖	喫茶小舖
單片機	微控制器
回調	回撥
固件	韌體
圖像	影像
圖庫	相簿
圖標	圖示
圖瓦盧	吐瓦魯
土庫曼斯坦	土庫曼
在線	線上
圭亞那	蓋亞那
地址	地址 位址
地址欄	位址列
坦桑尼亞	坦尚尼亞
埃塞俄比亞	衣索比亞
城域網	都會網路
基里巴斯	吉里巴斯
堆棧	堆疊
場效應管	場效電晶體
塑料	塑膠
塔吉克斯坦	塔吉克
塞拉利昂	獅子山
塞浦路斯	塞普勒斯
塞舌爾	塞席爾
壁紙	桌布 壁紙
外置	外接
外鍵	外部索引鍵
多任務	多工
多態	多型
多米尼加	多明尼加
多線程	多執行緒
奔馳	賓士
奶酪	乳酪
字庫	字型檔
字段	欄位
字符	字元
字符串	字串
字符集	字元集
字節	位元組
字節跳動	字節跳動
字體	字型
存儲	儲存
存盤	存檔
安提瓜和巴布達	安地卡及巴布達
宏	宏 巨集
宏內核	單核心
寄存器	暫存器
密鑰	金鑰
實例	例項 實例
實時	即時
實模式	真實模式
審覈	稽核
寫保護	防寫
寬帶	寬頻
尋址	定址
對話框	對話方塊
對象	物件 對象
導入	匯入
導出	匯出
尼日利亞	奈及利亞
尼日爾	尼日
局域網	區域網
局部	區域性
屏幕	螢幕
屏蔽	遮蔽
岡比亞	甘比亞
嵌套	巢狀
巴巴多斯	巴貝多
巴布亞新幾內亞	巴布亞紐幾內亞
布基納法索	布吉納法索
布爾	布林
布隆迪	蒲隆地
帕勞	帛琉
帶寬	頻寬
幾內亞比紹	幾內亞比索
幾率	機率
引導程序	載入程式
彙編	彙編 組譯
彙編語言	組合語言
後綴	字尾
循環	迴圈 循環
性價比	價效比
性能	效能
意大利	義大利
截取	擷取
截屏	截圖
所羅門羣島	索羅門群島
打印	列印
打印機	印表機
打開	開啟 打開
批量	批次
抑鬱症	憂鬱症
拋出	丟擲
拷貝	複製
持久性	永續性
指針	指標
捲積	摺積
掃描儀	掃描器
掛斷	結束通話
採樣	取樣
採樣率	取樣率
接口	介面
控件	控制元件
插件	外掛
搜索	搜尋
摩爾線程	摩爾線程
操作數	運算元
操作符	運算子
操作系統	作業系統
擴展	擴充套件
擴展名	副檔名
支持	支援
支持者	支持者
散列	雜湊
數字	數字 數位
數字印刷	數位印刷
數字電子	數位電子
數字電路	數位電路
數據	資料
數據倉庫	資料倉儲
數據報	資料包
數據庫	資料庫
數據挖掘	資料探勘
數據源	資料來源
數組	陣列
文件	檔案
文件名	檔名
文件夾	資料夾
文件擴展名	副檔名
文字處理	文書處理
文本	文字
文檔	文件
文萊	汶萊
斯威士蘭	史瓦濟蘭
斯洛文尼亞	斯洛維尼亞
新西蘭	紐西蘭
方便麪	泡麵 速食麵
方程式	方程式
映射	對映
時分多址	分時多重進接
時分複用	分時多工
時鐘頻率	時脈頻率
晶閘管	閘流體
晶體管	電晶體
智能	智慧
最終用戶	終端使用者
有損壓縮	有失真壓縮
服務器	伺服器
本地代碼	原生代碼
李彥宏	李彥宏
析構函數	解構函式
枚舉	列舉
查找	查詢
查看	檢視
格林納達	格瑞那達
格魯吉亞	喬治亞
桌面型	桌上型
概率	機率
構造函數	建構函式
標識符	識別符號
模塊	模組
模擬	模擬 類比
模擬電子	類比電子
模擬電路	類比電路
權限	許可權
歐拉	尤拉
正則表達式	正規表示式
死機	宕機
殺毒	防毒
比特	位元
比特幣	比特幣
比特率	位元率
毛里塔尼亞	茅利塔尼亞
毛里求斯	模里西斯
沙特阿拉伯	沙烏地阿拉伯
波分複用	波長分波多工
波斯尼亞黑塞哥維那	波士尼亞赫塞哥維納
津巴布韋	辛巴威
洪都拉斯	宏都拉斯
海內存知己	海內存知己
消息	訊息 消息
涼菜	冷盤
添加	新增
源代碼	原始碼
源文件	原始檔
源碼	原始碼
溢出	溢位
溫納圖萬	那杜
滾動條	捲軸
演示文稿	簡報
演算法	演算法
激光	雷射
激活	啟用
烏茲別克斯坦	烏茲別克
無損壓縮	無失真壓縮
物理內存	實體記憶體
物理地址	實體地址
特立尼達和多巴哥	千里達及托巴哥
狀態欄	狀態列
瑙魯	諾魯
瓦努阿圖	萬那杜
用戶	使用者
用戶名	使用者名稱
界面	介面
異步	非同步
登錄	登入
發佈	釋出
發送	傳送
的士	計程車
盤片	碟片
盤符	磁碟機代號
盧旺達	盧安達
目標代碼	目的碼
相冊	相簿
矢量	向量
知識產權	智慧財產權
短信	簡訊
砹	砈
硅	矽
硬件	硬體
硬盤	硬碟
碼分多址	分碼多重進接
碼率	位元速率
磁盤	磁碟
磁道	磁軌
社區	社群 社區
禁用	停用
科摩羅	葛摩
科特迪瓦	象牙海岸
移動硬盤	行動硬碟
移動網絡	行動網路
移動資料	行動資料
移動通信	行動通訊
移動電話	行動電話
程序	程式
程序不正義	程序不正義
程序員	程式設計師
程序正義	程序正義
空分多址	分空間多重進接
空分複用	空間多工
空氣淨化器	空氣清淨機
突尼斯	突尼西亞
窗口	視窗
端口	埠
筆記本電腦	筆記型電腦
算子	運算元
算法	演算法
範式	正規化
粘貼	貼上 粘貼
紅心大戰	傷心小棧
納米	奈米
純喫茶	純喫茶
索馬里	索馬利亞
組件	元件
綁定	繫結
網上鄰居	網路上的芳鄰
網卡	網絡卡
網吧	網咖
網絡	網路
網關	閘道器
線程	執行緒
編程	程式設計
編程語言	程式語言
緩存	快取
縮略圖	縮圖
縮進	縮排
總線	匯流排
缺省	預設
老撾	寮國
聖基茨和尼維斯	聖克里斯多福及尼維斯
聖文森特和格林納丁斯	聖文森及格瑞那丁
聖盧西亞	聖露西亞
聖馬力諾	聖馬利諾
聯繫	聯絡
聯繫歷史	通話記錄
聲卡	音效卡
聲明	宣告
肯尼亞	肯亞
胰腺	胰臟
脫機	離線
腳本	指令碼
自動轉屏	自動旋轉螢幕
自行車	腳踏車
臺式機	桌上型電腦
航天飛機	太空梭
芯片	晶片
花屏	破圖
英偉達	輝達
莫桑比克	莫三比克
菜單	選單 菜單
萊索托	賴索托
萬維網	全球資訊網
萬象	永珍
藍屏	藍色畫面
蘇里南	蘇利南
處理程序	處理程序
虛函數	虛擬函式
虛擬機	虛擬機器
虛擬機器	虛擬機器
表達式	表示式 運算式
複印	影印
複選按鈕	核取按鈕
複選框	核取方塊
視圖	檢視
視頻	影片 視訊
視頻會議	視訊會議
視頻通話	視訊通話
解釋器	直譯器
觸摸	觸控
觸摸屏	觸控式螢幕
計算機安全	電腦保安
計算機科學	電腦科學
訪問	訪問 存取
設備	裝置
設置	設定
註冊機	序號產生器
註冊表	登錄檔
註銷	登出
詞組	片語
調度	排程
調用	呼叫
調色板	調色盤
調製	調變
調製解調器	數據機
調試	除錯 偵錯
調試器	偵錯程式
變量	變數
貝寧	貝南
贊比亞	尚比亞
蹦極	高空彈跳
軟件	軟體
軟驅	軟碟機
軟體動物	軟體動物
輔音	子音
轉義字符	跳脫字元
通信	通訊
通訊卡	通話卡
通過	透過 通過
通配符	萬用字元
連接	連線
連接器	聯結器
進制	進位制
進程	程序 進程
運算符	運算子
運行	執行
過程式編程	程序式程式設計
遞歸	遞迴
遠程	遠端
適配器	介面卡
邏輯門	邏輯閘
酰	醯
重命名	重新命名
重裝	重灌
重載	過載
金屬氧化物半導體	金氧半導體
鈁	鍅
鈈	鈽
錄像	錄影
錇	鉳
鍀	鎝
鎄	鑀
鎇	鋂
鎿	錼
鏈接	連結
鏈表	連結串列
鏡像	映象
鐦	鉲
鑥	鎦
門戶網站	入口網站
門電路	閘電路
閃存	快閃記憶體
關係數據庫	關聯式資料庫
阿塞拜疆	亞塞拜然
阿拉伯聯合酋長國	阿拉伯聯合大公國
隊列	佇列
集成	整合
集成電路	積體電路
集羣	叢集
雲存儲	雲端儲存
雲計算	雲端計算
面向對象	物件導向
面向過程	程序導向
音頻	音訊
頁眉	頁首
頁腳	頁尾
項目	專案
預處理器	前處理器
頭文件	標頭檔案
頻分多址	分頻多重進接
頻分複用	分頻多工
類型	型別
類模板	類别範本
顯像管	映象管
顯卡	顯示卡
顯存	視訊記憶體
飛行模式	飛航模式
首席信息官	資訊長
首席執行官	執行長
首席技術官	技術長
首席運營官	營運長
香農	夏農
馬爾代夫	馬爾地夫
馬里共和國	馬利共和國
高性能計算	高效能運算
高清	高畫質
高端	高階 進階
高級	高階 進階 高級
高速緩存	快取記憶體
黃宏	黃宏
黑客	駭客
默認	預設
默認值	預設值
點擊	點選
鼠標	滑鼠
`;

// --- [區塊 B：自訂擴充覆寫詞庫] ---
const rawPhraseExpandedData = `
銀行內存	銀行內存
`;

// --- [##核心邏輯區##] ---
function main() {
    // 1. 動態模式與多國語言設定
    const langId = editor.LangID;
    const _L = (en, tc, sc) => {
        if (langId === 1028) return tc;
        if (langId === 2052) return sc;
        return en;
    };

    let msgModeNameTC = "", msgModeNameSC = "", msgModeNameEN = "";
    
    switch(CURRENT_MODE) {
        case "T2S":
            msgModeNameTC = "繁轉簡"; msgModeNameSC = "繁转简"; msgModeNameEN = "T2S"; break;
        case "S2T":
            msgModeNameTC = "簡轉繁"; msgModeNameSC = "简转繁"; msgModeNameEN = "S2T"; break;
        case "S2TE":
            msgModeNameTC = "簡轉繁: 擴充版"; msgModeNameSC = "简转繁: 扩充版"; msgModeNameEN = "S2T: Expanded"; break;
        case "S2TWP":
            msgModeNameTC = "用語轉換: 大陸 ➔ 台灣"; msgModeNameSC = "用语转换: 大陆 ➔ 台湾"; msgModeNameEN = "Terminology: CN->TW"; break;
        case "TW2SP":
            msgModeNameTC = "用語轉換: 台灣 ➔ 大陸"; msgModeNameSC = "用语转换: 台湾 ➔ 大陆"; msgModeNameEN = "Terminology: TW->CN"; break;
        default:
            msgModeNameTC = "文字"; msgModeNameSC = "文字"; msgModeNameEN = "Text"; break;
    }

    const MSG_NO_TEXT = _L("No text selected.", "未選取文字", "未选中文本");

    const getMsgProcessing = (p) => _L(
        `⏳ Processing... ${p}%`, 
        `⏳ ${msgModeNameTC}處理中... ${p}%`, 
        `⏳ ${msgModeNameSC}处理中... ${p}%`
    );

    const getMsgSuccess = (count, time) => {
        let suffixTC = (CURRENT_MODE === "S2T" || CURRENT_MODE === "S2TE") ? "已套用台灣字形，" : "";
        let suffixSC = (CURRENT_MODE === "S2T" || CURRENT_MODE === "S2TE") ? "已套用台湾字形，" : "";
        return _L(
            `✅ Conversion completed (${msgModeNameEN}, ${count} ops in ${time} s).`,
            `✅ 轉換完成 (${msgModeNameTC}，${suffixTC}共取代 ${count} 處，耗時 ${time} 秒)`,
            `✅ 转换完成 (${msgModeNameSC}，${suffixSC}共替换 ${count} 处，耗时 ${time} 秒)`
        );
    };

    const startTime = Date.now();

    // 2. 選取與防呆處理 (穩定版)
    let isCSVCellSel = false;

    try {
        if ((document.selection.Mode & 15) === 4) isCSVCellSel = true;
    } catch(e) {}

    if (!isCSVCellSel && document.selection.IsEmpty) {
        let checkText = "";
        try { 
            checkText = document.selection.Text; 
        } catch(e) {}
        
        if (checkText.length > 0) {
            isCSVCellSel = true;
        }
    }

    if (!isCSVCellSel && document.selection.IsEmpty) {
        document.selection.SelectWord();
        if (document.selection.IsEmpty) {
            alert(MSG_NO_TEXT); 
            Quit();
        }
    }

    const topY = document.selection.GetTopPointY(eePosLogical);
    const topX = document.selection.GetTopPointX(eePosLogical);
    const bottomY = document.selection.GetBottomPointY(eePosLogical);
    const bottomX = document.selection.GetBottomPointX(eePosLogical);
    const selectedLines = bottomY - topY + 1;

    if (!isCSVCellSel && selectedLines > 15000000) {
        alert(_L(
            "⚠️ Selected lines exceed 15,000,000 limit. Conversion aborted. Please reduce selection.",
            "⚠️ 選取總行數超過 1500 萬行，超出安全轉換限制。\n為避免系統無回應，請減少選取範圍後再試。",
            "⚠️ 选取总行数超过 1500 万行，超出安全转换限制。\n为避免系统无回应，请减少选取范围后再试。"
        ));
        Quit();
    }

    let isFilterMode = false;
    try {
        if (document.filters && document.filters.Count > 0) {
            isFilterMode = true;
        }
    } catch (e) {}

    let selCount = 1;
    let isMultiSel = false;
    let isVerticalSel = false; 

    try {
        if ((document.selection.Mode & 15) === 3) {
            isVerticalSel = true;
            isMultiSel = true; 
        }
    } catch(e) {}

    try {
        if (typeof document.selection.Count !== "undefined" && document.selection.Count > 1) {
            selCount = document.selection.Count;
            isMultiSel = true;
        }
    } catch(e) {}

    if (!isCSVCellSel && (isFilterMode || isMultiSel) && selectedLines > 5000) {
        alert(_L(
            "Selection exceeds 5000 lines limit for Filter/Multi-Select mode. Please reduce selection.",
            "⚠️ 篩選或多段選取模式下，選取範圍超過 5000 行限制。\n為避免系統無回應，請減少選取範圍後再試。",
            "⚠️ 筛选或多段选取模式下，选取范围超过 5000 行限制。\n为避免系统无回应，请减少选取范围后再试。"
        ));
        Quit();
    }

    // 3. 切塊邏輯
    let chunks = [];
    if (!isCSVCellSel && !isFilterMode && !isMultiSel) {
        let numChunks = 1;
        if (selectedLines > 5000000) numChunks = 50;
        else if (selectedLines > 10000) numChunks = 20;
        else if (selectedLines > 2000) numChunks = 5;

        let linesPerChunk = Math.ceil(selectedLines / numChunks);
        if (linesPerChunk < 1) linesPerChunk = 1;

        let currY = topY;
        while (currY <= bottomY) {
            let eY = Math.min(currY + linesPerChunk - 1, bottomY);
            chunks.push({
                topY: currY, bottomY: eY,
                isFirst: (currY === topY), isLast: (eY === bottomY)
            });
            currY = eY + 1;
        }
        chunks.reverse(); 
    }

    // 4. 動態構建 Trie 引擎
    status = _L(
        `⏳ Building Trie Engine & Processing (${msgModeNameEN})...`, 
        `⏳ 建立字典樹引擎，${msgModeNameTC}處理中...`, 
        `⏳ 建立字典树引擎，${msgModeNameSC}处理中...`
    );

    const variantMap = new Map();
    const finalDict = new Map();

    // --- [初始化區塊 E 例外名單防火牆] ---
    const exceptionSet = new Set();
    const exceptionMap = new Map();
    try {
        const linesEx = rawExceptionData.split("\n");
        for (let line of linesEx) {
            let trimmed = line.trim();
            if (!trimmed || trimmed.startsWith("#")) continue;
            let parts = trimmed.split("\t");
            if (parts.length >= 2) {
                let key = parts[0];
                let target = parts[1];
                exceptionSet.add(key);
                exceptionSet.add(target);
                exceptionMap.set(key, target);
                exceptionMap.set(target, target);
            }
        }
    } catch(e) {}

    const needVariants = (CURRENT_MODE === "S2T" || CURRENT_MODE === "S2TE");
    
    if (needVariants) {
        try {
            const linesTW = rawTWVariants.split("\n");
            for (let line of linesTW) {
                let trimmed = line.trim();
                if (!trimmed || trimmed.startsWith("#")) continue;
                let parts = trimmed.split("\t");
                if (parts.length >= 2) variantMap.set(parts[0], parts[1].split(" ")[0]);
            }
        } catch(e) {}
    }

    const applyTWVariants = (str) => {
        if (!needVariants) return str; 
        let res = "";
        for (let c of str) res += variantMap.has(c) ? variantMap.get(c) : c;
        return res;
    };

    const parseMainData = (rawStr) => {
        if (!rawStr) return;
        const lines = rawStr.split("\n");
        for (let line of lines) {
            let trimmed = line.trim();
            if (!trimmed || trimmed.startsWith("#")) continue;
            let parts = trimmed.split("\t");
            if (parts.length >= 2) {
                let key = parts[0];
                let rawTarget = parts[1].split(" ")[0];

                let target;

                if (exceptionMap && exceptionMap.has(key)) {
                    target = exceptionMap.get(key);
                } else {
                    target = applyTWVariants(rawTarget);
                }

                if (!finalDict.has(key)) {
                    finalDict.set(key, target);
                }
            }
        }
    };

    try { parseMainData(rawExceptionData); } catch(e){}
    try { parseMainData(rawPhraseExpandedData); } catch(e){}
    try { parseMainData(rawPhraseData); } catch(e){}
    
    if (CURRENT_MODE !== "S2TWP" && CURRENT_MODE !== "TW2SP") {
        try { parseMainData(rawCharData); } catch(e){}
    }

    if (needVariants) {
        for (let [vKey, vTarget] of variantMap) {
            if (!finalDict.has(vKey)) finalDict.set(vKey, vTarget);
        }
    }

    const trie = Object.create(null);
    for (let [key, target] of finalDict) {
        let node = trie;
        for (let j = 0; j < key.length; j++) {
            let char = key[j];
            if (node[char] === undefined) node[char] = Object.create(null);
            node = node[char];
        }
        node.isWord = true;
        node.target = target;
    }

    const doConvert = (srcText) => {
        let textLen = srcText.length;
        let out = []; 
        let i = 0;
        let lastMatchEnd = 0;
        let replaceCount = 0;

        while (i < textLen) {
            let node = trie;
            let longestMatchLen = 0;
            let longestMatchTarget = null;
            let j = i;

            while (j < textLen) {
                let char = srcText[j];
                node = node[char];
                if (node === undefined) break; 
                if (node.isWord) {
                    longestMatchLen = j - i + 1;
                    longestMatchTarget = node.target;
                }
                j++;
            }

            if (longestMatchLen > 0) {
                if (i > lastMatchEnd) out.push(srcText.substring(lastMatchEnd, i));
                let originalText = srcText.substring(i, i + longestMatchLen);
                if (originalText !== longestMatchTarget) replaceCount++;
                out.push(longestMatchTarget);
                i += longestMatchLen;
                lastMatchEnd = i;
            } else { i++; }
        }
        if (lastMatchEnd < textLen) out.push(srcText.substring(lastMatchEnd, textLen));
        return { text: out.join(""), count: replaceCount };
    };

    // 5. 開始轉換
    let totalReplaced = 0;
    let bOrgCombine = false; 

    try { Redraw = false; } catch(e) {}
    try { 
        bOrgCombine = CombineHistory;
        CombineHistory = true; 
    } catch(e) {} 

    try {
        if (isCSVCellSel) {
            const eePosCell = 4;
            const eePosLogical = 1;
            let cTopRow, cBottomRow, cTopCol, cBottomCol;
            try {
                cTopRow = document.selection.GetTopPointY(eePosCell);
                cBottomRow = document.selection.GetBottomPointY(eePosCell);
                cTopCol = document.selection.GetTopPointX(eePosCell);
                cBottomCol = document.selection.GetBottomPointX(eePosCell);
            } catch(e) {
                status = "";
                alert(_L("❌ Error reading cell coordinates.", "❌ 無法取得儲存格座標！", "❌ 无法取得单元格坐标！"));
                Quit();
            }

            let totalCells = (cBottomRow - cTopRow + 1) * (cBottomCol - cTopCol + 1);
            let delimChar = "\t"; 
            try {
                if (document.Csv) delimChar = document.Csv.Delimiter;
            } catch(e) {}

            let origTopY = document.selection.GetTopPointY(eePosLogical);
            let origTopX = document.selection.GetTopPointX(eePosLogical);
            let origBotY = document.selection.GetBottomPointY(eePosLogical);
            let origBotX = document.selection.GetBottomPointX(eePosLogical);

            document.selection.SetActivePoint(eePosCell, 1, cTopRow);
            document.selection.SetActivePoint(eePosCell, cBottomCol, cBottomRow, true);
            let fullText = document.selection.Text;

            document.selection.SetActivePoint(eePosCell, cTopCol, cTopRow);
            document.selection.SetActivePoint(eePosCell, cBottomCol, cBottomRow, true);

            let isJagged = false;
            let cleanText = fullText.replace(/[\r\n]+$/, "");
            let lines = cleanText.split(/\r?\n/);
            let requiredDelims = cBottomCol - 1;

            for (let i = 0; i < lines.length; i++) {
                let line = lines[i];
                let count = 0;
                let inQuotes = false;

                for (let j = 0; j < line.length; j++) {
                    let char = line[j];
                    if (char === '"') inQuotes = !inQuotes;
                    else if (char === delimChar && !inQuotes) count++;
                }

                if (count < requiredDelims) {
                    isJagged = true;
                    break;
                }
            }

            if (lines.length < (cBottomRow - cTopRow + 1)) {
                isJagged = true;
            }

            if (isJagged) {
                if (totalCells > 10000) {
                    status = "";
                    alert(_L(
                        "⚠️ Irregular CSV selection (>10,000 cells) detected.\nConversion aborted to prevent data shifting. Please select a smaller area or a perfect rectangle.",
                        "⚠️ 偵測到不規則的 CSV 選取範圍，且超過 10,000 格限制。\n為防止轉換後發生資料推擠錯位，系統已自動攔截轉換。請縮小選取範圍，或選取完整矩形。",
                        "⚠️ 侦测到不规则的 CSV 选取范围，且超过 10,000 格限制。\n为防止转换后发生数据推挤错位，系统已自动拦截转换。请缩小选取范围，或选取完整矩形。"
                    ));
                    Quit();
                } else {
                    ShowTip(_L(
                        "📊 Irregular CSV detected, preparing precise conversion...", 
                        "📊 偵測到不規則的 CSV 選取範圍，精確轉換準備中...", 
                        "📊 侦测到不规则的 CSV 选取范围，精确转换准备中..."
                    ), 1);

                    
                    let pendingCells = [];

                    for (let r = cTopRow; r <= cBottomRow; r++) {
                        for (let c = cTopCol; c <= cBottomCol; c++) {
                            let cellText = "";
                            try {
                                cellText = document.GetCell(r, c, 0); 
                            } catch(e) {
                                continue;
                            }

                            if (cellText) {
                                if (cellText.length > 25000000) {
                                    status = "";
                                    alert(_L("⚠️ Selection contains a giant cell.", "⚠️ 選取範圍包含超過 50MB 的巨型儲存格，停止轉換。", "⚠️ 选取范围包含超过 50MB 的巨型单元格，停止转换。"));
                                    Quit();
                                }
                                let res = doConvert(cellText);
                                if (res.count > 0) {
                                    pendingCells.push({ r: r, c: c, text: res.text, count: res.count });
                                }
                            }
                        }
                    }

                    let tCells = pendingCells.length;
                    for (let i = 0; i < tCells; i++) {
                        if (i % Math.max(1, Math.floor(tCells / 9)) === 0) {
                            ShowTip(getMsgProcessing(Math.round(((i + 1) / tCells) * 99)), 1);
                        }
                        let item = pendingCells[i];
                        try {
                            document.SetCell(item.r, item.c, item.text, 0); 
                            totalReplaced += item.count;
                        } catch(e) {}
                    }

                    try {
                        document.selection.SetActivePoint(eePosCell, cTopCol, cTopRow);
                        document.selection.SetActivePoint(eePosCell, cBottomCol, cBottomRow, true);
                    } catch(e) {}
                    ShowTip("", eeShowTipHide);
                }
            } else {
                let srcText = document.selection.Text;

                if (srcText && srcText.length > 150000000) {
                    status = "";
                    alert(_L("⚠️ Selection contains a giant chunk (> 300MB). Conversion aborted.", "⚠️ 選取範圍包含超過 300MB 的單一巨型資料區塊，為防止記憶體溢位，停止轉換。", "⚠️ 选取范围包含超过 300MB 的单一巨型数据区块，为防止内存溢出，停止转换。"));
                    Quit();
                }

                ShowTip(_L("📊 CSV mode processing...", "📊 CSV模式轉換處理中...", "📊 CSV模式转换处理中..."), 1);

                if (srcText) {
                    let hasTrailingNewline = /[\r\n]+$/.test(srcText);
                    let res = doConvert(srcText);
                    if (res.count > 0) {
                        let outText = res.text;
                        if (hasTrailingNewline) {
                            outText = outText.replace(/[\r\n]+$/, "");
                        }
                        document.selection.Text = outText;
                        totalReplaced += res.count;
                    }
                }
                ShowTip("", eeShowTipHide);
            }

        } else if (isFilterMode) {
            let blocks = [];
            if (isVerticalSel && selCount === 1) {
                let minX = Math.min(topX, bottomX);
                let maxX = Math.max(topX, bottomX);
                for (let y = topY; y <= bottomY; y++) {
                    blocks.push({
                        ty: y, tx: minX, by: y, bx: maxX
                    });
                }
            } else if (isMultiSel) {
                for (let i = 1; i <= selCount; i++) {
                    try {
                        blocks.push({
                            ty: document.selection.GetTopPointY(eePosLogical, i),
                            tx: document.selection.GetTopPointX(eePosLogical, i),
                            by: document.selection.GetBottomPointY(eePosLogical, i),
                            bx: document.selection.GetBottomPointX(eePosLogical, i)
                        });
                    } catch(e) {}
                }
            } else {
                blocks.push({ ty: topY, tx: topX, by: bottomY, bx: bottomX });
            }

            let pendingWrites = [];
            for (let c = 0; c < blocks.length; c++) {
                let b = blocks[c];
                for (let y = b.ty; y <= b.by; y++) {
                    let lineStr = document.GetLine(y); 
                    let lineLen = lineStr.length; 
                    let startX = (y === b.ty) ? b.tx : 1;
                    let endX = (y === b.by) ? b.bx : (lineLen + 1);

                    if (startX > lineLen + 1) startX = lineLen + 1;
                    if (endX > lineLen + 1) endX = lineLen + 1;
                    if (startX >= endX) continue;

                    let textToProcess = lineStr.substring(startX - 1, endX - 1);
                    if (!textToProcess) continue;

                    if (textToProcess.length > 25000000) {
                        status = "";
                        alert(_L("⚠️ Selection contains a giant single line (> 50MB). Conversion aborted.", "⚠️ 選取範圍包含超過 50MB 的單一巨型資料行，為防止記憶體溢位，停止轉換。", "⚠️ 选取范围包含超过 50MB 的单一巨型数据行，为防止内存溢出，停止转换。"));
                        Quit();
                    }

                    let res = doConvert(textToProcess);
                    if (res.count > 0) pendingWrites.push({ y: y, startX: startX, endX: endX, text: res.text, count: res.count });
                }
            }

            pendingWrites.sort(function(a, b) {
                if (b.y !== a.y) return b.y - a.y;
                return b.startX - a.startX;
            });

            let totalW = pendingWrites.length;
            for (let w = 0; w < totalW; w++) {
                if (w % Math.max(1, Math.floor(totalW / 9)) === 0) {
                    ShowTip(getMsgProcessing(Math.round(((w + 1) / totalW) * 99)), 1);
                }
                let item = pendingWrites[w];
                try {
                    document.selection.SetActivePoint(eePosLogical, item.startX, item.y);
                    if (document.selection.GetActivePointY(eePosLogical) === item.y) {
                        document.selection.SetActivePoint(eePosLogical, item.endX, item.y, true);
                        document.selection.Text = item.text;
                        totalReplaced += item.count;
                    }
                } catch(e) {}
            }
            ShowTip("", eeShowTipHide);

        } else if (isMultiSel) {
            let multiBlocks = [];
            
            if (isVerticalSel && selCount === 1) {
                let minX = Math.min(topX, bottomX);
                let maxX = Math.max(topX, bottomX);
                for (let y = topY; y <= bottomY; y++) {
                    multiBlocks.push({ ty: y, tx: minX, by: y, bx: maxX });
                }
            } else {
                for (let i = 1; i <= selCount; i++) {
                    try {
                        multiBlocks.push({
                            ty: document.selection.GetTopPointY(eePosLogical, i),
                            tx: document.selection.GetTopPointX(eePosLogical, i),
                            by: document.selection.GetBottomPointY(eePosLogical, i),
                            bx: document.selection.GetBottomPointX(eePosLogical, i)
                        });
                    } catch (e) {}
                }
            }
            multiBlocks.reverse();

            let totalM = multiBlocks.length;
            for (let i = 0; i < totalM; i++) {
                if (i % Math.max(1, Math.floor(totalM / 9)) === 0) {
                    ShowTip(getMsgProcessing(Math.round(((i + 1) / totalM) * 99)), 1);
                }
                let b = multiBlocks[i];
                try {
                    document.selection.SetActivePoint(eePosLogical, b.tx, b.ty);
                    document.selection.SetActivePoint(eePosLogical, b.bx, b.by, true);
                    let srcText = document.selection.Text;
                    if (!srcText) continue;

                    if (srcText.length > 25000000) {
                        status = "";
                        alert(_L("⚠️ Selection contains a giant chunk (> 50MB). Conversion aborted.", "⚠️ 選取範圍包含超過 50MB 的單一巨型資料區塊，為防止記憶體溢位，停止轉換。", "⚠️ 选取范围包含超过 50MB 的单一巨型数据区块，为防止内存溢出，停止转换。"));
                        Quit();
                    }

                    let res = doConvert(srcText);
                    if (res.count > 0) {
                        document.selection.Text = res.text;
                        totalReplaced += res.count;
                    }
                } catch (e) { continue; }
            }
            ShowTip("", eeShowTipHide);

        } else {
            const totalChunks = chunks.length;
            for (let c = 0; c < totalChunks; c++) {
                let progress = Math.round(((c + 1) / totalChunks) * 99);
                ShowTip(getMsgProcessing(progress), 1);

                let chunk = chunks[c];
                if (chunk.isFirst) {
                    document.selection.SetActivePoint(eePosLogical, topX, chunk.topY);
                } else {
                    document.selection.SetActivePoint(eePosLogical, 1, chunk.topY);
                }
                if (chunk.isLast) {
                    document.selection.SetActivePoint(eePosLogical, bottomX, chunk.bottomY, true);
                } else {
                    document.selection.SetActivePoint(eePosLogical, 1, chunk.bottomY + 1, true);
                }

                let srcText = "";
                try {
                    srcText = document.selection.Text;
                } catch (e) {
                    status = "";
                    alert(_L(`❌ Crash at chunk ${c + 1}.`, `❌ 第 ${c + 1} 區塊失敗。`, `❌ 第 ${c + 1} 区块失败。`));
                    Quit();
                }
                if (!srcText) continue;

                if (srcText.length > 25000000 && chunk.topY === chunk.bottomY) {
                    status = "";
                    alert(_L("⚠️ Selection contains a giant single line (> 50MB). Conversion aborted.", "⚠️ 選取範圍包含超過 50MB 的單一巨型資料行，為防止記憶體溢位，停止轉換。", "⚠️ 选取范围包含超过 50MB 的单一巨型数据行，为防止内存溢出，停止转换。"));
                    Quit();
                }

                let res = doConvert(srcText);
                if (res.count > 0) {
                    try {
                        document.selection.Text = res.text;
                        totalReplaced += res.count;
                    } catch (e) {
                        status = "";
                        alert(_L("Memory error.", "寫入時發生記憶體不足。", "写入时发生内存不足。"));
                        Quit();
                    }
                }
            }
            ShowTip("", eeShowTipHide);
        }

        if (!isCSVCellSel) {
            if (!isFilterMode && !isMultiSel) {
                document.selection.SetActivePoint(eePosLogical, topX, topY);
                document.selection.SetActivePoint(eePosLogical, bottomX, bottomY, true);
            } else {
                document.selection.SetActivePoint(eePosLogical, topX, topY);
            }
        }

    } finally {
        try { CombineHistory = bOrgCombine; } catch(e) {} 
        try { Redraw = true; } catch(e) {}

        if (totalReplaced > 0 && !isCSVCellSel && document.Csv && document.Csv.Delimiter !== "") {
            editor.ExecuteCommandByID(3967);
            editor.ExecuteCommandByID(3967);
            shell.SendKeys( "{LEFT}" ); 
            shell.SendKeys( "{RIGHT}" ); 
        }

        finalDict.clear();
    }

    // 6. 收尾與計時
    const endTime = Date.now();
    const elapsedSeconds = ((endTime - startTime) / 1000).toFixed(2);

    if (totalReplaced > 0) {
        status = getMsgSuccess(totalReplaced, elapsedSeconds); 
    } else {
        status = _L(
            `ℹ️ No text requiring changes was found, so no conversion was made (${elapsedSeconds} s).`, 
            `ℹ️ 內容未包含需要變更的文字，未執行 ${msgModeNameTC} 轉換 (耗時 ${elapsedSeconds} 秒)`, 
            `ℹ️ 内容未包含需要变更的文字，未执行 ${msgModeNameSC} 转换 (耗时 ${elapsedSeconds} 秒)`
        );
    }
}

main();